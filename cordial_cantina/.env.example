# Cordial Cantina Environment Variables
# Copy this file to .env and fill in actual values
# NEVER commit .env to source control

# === Phoenix Server ===
# Port for the web server (default: 4000)
PORT=4000

# Set to "true" to start the Phoenix server (required for releases)
# PHX_SERVER=true

# === Production Only ===
# These are required in production but have defaults in dev/test

# Secret key for signing cookies and sessions
# Generate with: mix phx.gen.secret
# SECRET_KEY_BASE=your_secret_key_here

# Host for URL generation in production
# PHX_HOST=example.com

# DNS cluster query for distributed Erlang (optional)
# DNS_CLUSTER_QUERY=

# === API Keys (Future) ===
# Birdeye API key for market data (V0.1-M4)
# BIRDEYE_API_KEY=your_birdeye_api_key

# Raydium API key if required (V0.2+)
# RAYDIUM_API_KEY=your_raydium_api_key

# === Database ===
# PostgreSQL connection URL (required for production)
# Development uses postgres/postgres@localhost by default
# DATABASE_URL=postgres://user:pass@localhost/cordial_cantina_dev

# Connection pool size (default: 10)
# POOL_SIZE=10

# Enable IPv6 for Ecto connections
# ECTO_IPV6=true
